# Tasks: Native Win32 Tray Icon

- [ ] T1 Draw the Planlet-defined cobalt keycap, microphone, and opposing-arrow concept from original primitive geometry, commit its editable vector master and hand-tuned multi-resolution `.ico`, configure it as the executable icon, and verify the published Native AOT executable contains a legible light-and-dark-taskbar icon without a sidecar asset or third-party artwork
- [ ] T2 Add the direct Win32 notification-icon lifecycle and its narrow injectable add-result decision seam, replace the message-only window with an invisible top-level owner, route initial and `TaskbarCreated` re-add failures through visible error reporting and shared orderly shutdown, and recreate the icon after successful `TaskbarCreated` handling
- [ ] T3 Add the dynamic tooltip and right-click or keyboard context menu showing the application name, current Idle or Dictating status, `Ctrl+Alt+H`, and Exit, leaving left-click and double-click inert and requiring menu activation during Dictating to run normal focus-loss recovery before an Idle menu is rendered or cancelled
- [ ] T4 Implement one idempotent graceful-shutdown coordinator that blocks new commands but retains the timer, WinEvent hook, hidden window, message loop, icon, and callbacks through active or already-pending stop confirmation; complete teardown only after bounded late-popup correction confirms Voice Typing absent and layout restored, or cancel Exit visibly while keeping the utility running if closure cannot be confirmed
- [ ] T5 Update the README and tray-icon section of the concept document, and add focused automated coverage proving menu activation synchronizes Idle after focus recovery, shutdown does not tear down while `StopConfirmPending`, a late popup receives corrective handling, stable completion permits teardown, an unconfirmed bounded stop cancels Exit without releasing monitoring resources, and injected initial-add or `TaskbarCreated` re-add failures each report visibly and reach shared orderly termination without an invisible background process
- [ ] T6 Run build and test verification, including both controlled notification-icon add-failure cases, run Native AOT publish verification, then manually verify tray rendering, successful Explorer restart recovery, menu activation and cancellation while Dictating, Exit during bar launch and while `StopConfirmPending`, late-popup correction before successful Exit, exact layout restoration, and absence of visible window surfaces
