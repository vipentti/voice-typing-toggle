# Tasks: Listening disable toggle and Ctrl+Alt+H checkbox

- [ ] T1 Add the master "Enable listening" tray toggle to Program.cs: `ListeningEnabled` intent flag (default true); a shared abort helper that first completes any in-flight Win+H-hold gesture when `WinHHoldArmed` (never leave an injected Win held, never send a stray Escape into an unrelated foreground window) and then runs the canonical `Core.StopDictation()` Escape-first stop when dictating; disabling unregisters the Ctrl+Alt+H hotkey (when enabled), uninstalls the keyboard hook, and kills the focus-watch and Win+H-hold timers; re-enabling restores state per checkbox intent in dependency order (focus-watch timer, keyboard hook when `InterceptWinHEnabled`, hotkey when `HotkeyEnabled`) and, when hotkey registration fails, shows the startup-style message box and stays disabled (fail-closed, all-or-nothing); menu item with live `MF_CHECKED` state, "Status: Disabled" status line and tooltip while off, the "Enable Ctrl+Alt+H", "Intercept Win+H", "Close dictation on Enter", and "Close dictation on Space" items appended `MF_GRAYED | MF_DISABLED` and unselectable while off (their intent values preserved), trace events for enable/disable, and consistent teardown through the existing `CompleteShutdown` guards.
- [ ] T2 Add the "Enable Ctrl+Alt+H" tray checkbox: `HotkeyEnabled` intent flag (default false); startup no longer registers the hotkey and the fatal hotkey-in-use startup path is removed; the menu item shows the live state; checking registers the hotkey and shows the startup-style message box with the item staying unchecked on failure; unchecking while dictating first runs the canonical Escape-first stop and while a Win+H-hold gesture is armed completes that gesture first, then unregisters; the existing `WmHotkey` dispatch is unchanged when the hotkey is registered.
- [ ] T3 Update docs: revise the AGENTS.md guardrail "The normal toggle hotkey is Ctrl+Alt+H and stays always active" to tray-gated and off by default, and update the concept doc's hotkey and tray sections to describe both session-only toggles (master "Enable listening" and "Enable Ctrl+Alt+H") and the disabled-state behavior.
- [ ] T4 Run `dotnet build VoiceTypingToggle.slnx`, `dotnet test VoiceTypingToggle.slnx`, and `dotnet publish src\VoiceTypingToggle\VoiceTypingToggle.csproj -c Release`; all must pass with warnings and analyzer findings as errors.
- [ ] T5 Run the manual Windows verification matrix from plan.md: default start (Ctrl+Alt+H unregistered and reaching other applications untouched, physical Win+H still intercepts), checking and unchecking "Enable Ctrl+Alt+H" while Idle and while Dictating (disable stops and restores first), master disable while Idle and while Dictating (Escape-like stop, layout and focus restored, everything inert afterwards, tray still usable), re-enable restoring per-checkbox state, grayed sub-toggles unselectable, hotkey-in-use failure at enable and re-enable (message box, stays unchecked), disable during an in-flight Win+H hold (no stuck Win, no stray Escape), Exit while disabled, repeated toggle cycles, focus changes, and shutdown restoration.
