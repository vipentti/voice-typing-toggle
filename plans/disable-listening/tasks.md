# Tasks: Listening disable toggle and Ctrl+Alt+H checkbox

- [ ] T1 Add the master "Enable listening" tray toggle to Program.cs: `ListeningEnabled` intent flag (default true); `ShowTrayMenu` keeps its existing dictation handling unchanged (when `Core.IsDictating` it restores focus to the app window and runs `CheckDictationFocus`, so the focus-loss self-heal ends the session and restores state before the menu renders and commands are selected while the core is Idle in normal operation); a shared abort helper that guards every disable path for a still-active session, first resolving any in-flight Win+H-hold gesture when `WinHHoldArmed` through the defined hold-completion path (core still dictating: `CompleteWinHInjection()` as today, then the Escape-first stop closes the bar; core already Idle: complete the H down/up and Win up key sequence without the immediate closing Escape, arm the stop-confirmation watchdog when not already pending, and close the bar opened by the completed gesture only on positive evidence via the existing corrective machinery with its usual bounds; never leave an injected Win held, never send an Escape into the window that replaced the dictation target) and then running the canonical `Core.StopDictation()` Escape-first stop when dictating; disabling unregisters the Ctrl+Alt+H hotkey (when enabled), uninstalls the keyboard hook, and kills the focus-watch and Win+H-hold timers; `RequestOrderlyShutdown` re-arms the focus-watch timer when `ShutdownPolicy.Begin` returns Wait and the timer is not running, so a pending stop confirmation drains through the existing watchdog expiry and `ContinueShutdownIfNeeded` instead of stalling shutdown while disabled (`CompleteShutdown` kills the re-armed timer as it already does); re-enabling restores state per checkbox intent in dependency order (focus-watch timer, keyboard hook when `InterceptWinHEnabled`, hotkey when `HotkeyEnabled`); a failed hook installation at re-enable is nonfatal: it clears `InterceptWinHEnabled` to false (checkbox unchecked, physical Win+H native) and the re-enable continues; when hotkey registration fails at re-enable, rolls the just-applied hook and timer back, shows the startup-style message box, and ends with `ListeningEnabled` false and `HotkeyEnabled` false (fail-closed, all-or-nothing); menu item with live `MF_CHECKED` state, "Status: Disabled" status line and tooltip while off, the "Enable Ctrl+Alt+H", "Intercept Win+H", "Close dictation on Enter", and "Close dictation on Space" items appended `MF_GRAYED | MF_DISABLED` and unselectable while off (their intent values preserved across the disable/re-enable cycle), trace events for enable/disable, and consistent teardown through the existing `CompleteShutdown` guards.
- [ ] T2 Add the "Enable Ctrl+Alt+H" tray checkbox: `HotkeyEnabled` intent flag (default false); startup no longer registers the hotkey and the fatal hotkey-in-use startup path is removed; the menu item shows the live state; checking registers the hotkey and, on failure, shows the startup-style message box and ends with `HotkeyEnabled` false (item unchecked) while `ListeningEnabled` stays true and all other listening machinery is unchanged; unchecking while dictating runs the shared abort helper first (normally the session already ended via the tray-menu focus-loss heal before the command is selected; the helper is the defensive path when a session is still active) and while a Win+H-hold gesture is armed resolves it through the defined hold-completion path, then unregisters; the existing `WmHotkey` dispatch is unchanged when the hotkey is registered.
- [ ] T3 Update docs: revise the AGENTS.md guardrail "The normal toggle hotkey is Ctrl+Alt+H and stays always active" to tray-gated and off by default, and update the concept doc's hotkey and tray sections to describe both session-only toggles (master "Enable listening" and "Enable Ctrl+Alt+H") and the disabled-state behavior.
- [ ] T4 Run `dotnet build VoiceTypingToggle.slnx`, `dotnet test VoiceTypingToggle.slnx`, and `dotnet publish src\VoiceTypingToggle\VoiceTypingToggle.csproj -c Release`; all must pass with warnings and analyzer findings as errors.
- [ ] T5 Run the manual Windows verification matrix from plan.md: default start (Ctrl+Alt+H unregistered and reaching other applications untouched, physical Win+H still intercepts), checking and unchecking "Enable Ctrl+Alt+H" while Idle, and the dictating cases from the state that actually exists when menu commands run (opening the tray menu while Dictating ends the session via the focus-loss self-heal: bar closes, saved layout restored without stealing focus back, core Idle, status line shows Idle; the toggle is then selected while Idle), master disable while Idle and the same menu-open-during-dictation flow (session ends and restores first, everything inert afterwards, tray still usable), re-enable restoring per-checkbox state, grayed sub-toggles unselectable, hotkey-in-use failure at direct enable (message box, item unchecked, listening stays enabled) and at master re-enable (message box, application stays disabled with `ListeningEnabled` and `HotkeyEnabled` both false), Enter and Space close behavior and stop-flash watchdog behavior with listening active after a disable/re-enable cycle, disable during an in-flight Win+H hold with the core still dictating (gesture completes, Escape-first stop, no stuck Win) and with the core already healed Idle (start dictation, open the tray menu within the ~500 ms hold window so the heal returns the core to Idle while the hold is still armed, then disable: injected keys complete without an immediate Escape, no stuck Win, no Escape in the window that replaced the dictation target, bar closed on positive evidence or expiring under the documented positive-only limitation), Exit while disabled with no pending confirmation (immediate completion) and with a pending stop confirmation (stop dictation, disable listening before the watchdog expires, select Exit: the re-armed focus-watch timer drains the watchdog and shutdown completes or cancels under the existing semantics, never stalls), repeated toggle cycles, focus changes, and shutdown restoration; session-only reset: change checkbox intents (for example enable "Enable Ctrl+Alt+H" and disable listening), restart the application, and verify every checkbox returns to its documented default (hotkey unchecked, listening and Intercept Win+H checked); the defensive abort path (session still active when a toggle runs because the heal did not fire) is not reliably reproducible manually and is covered by code review and trace events, and hook installation failure (`SetWindowsHookExW` rarely fails) is likewise not reliably reproducible manually and is covered by code review and the existing `hook-install-failed` trace event, so the matrix targets the normal menu flow.
