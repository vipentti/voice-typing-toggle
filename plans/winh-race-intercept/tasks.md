# Tasks: Physical Win+H race interception

- [ ] T1 Add WH_KEYBOARD_LL hook scaffold: `SetWindowsHookExW`, `CallNextHookEx`, `UnhookWindowsHookEx`, and `KBDLLHOOKSTRUCT` P/Invoke in Program.cs; a low-level callback that tracks `VK_LWIN`/`VK_RWIN` down/up state from the hook events themselves (never `GetKeyState`), matches physical Win+H (first non-injected H keydown while a tracked Win flag is set), ignores `LLKHF_INJECTED` events, suppresses auto-repeat H keydowns and re-arms on H keyup or either Win keyup (exactly one observation per chord), chains through `CallNextHookEx` and returns its result on every pass-through path, handles `nCode < 0` by immediately chaining, never returns a nonzero value to swallow input, is exception-safe, never inspects key content, and never blocks or injects input from the callback; trace events for hook install, uninstall, and each Win+H observation; uninstall in `CompleteShutdown`.
- [ ] T2 Add race-start and native-stop entries to ToggleCore: race-start performs the existing layout request and wait but skips `SendWinH`; native-stop arms the existing stop-confirmation watchdog, marks Idle, defers restoration (saved window and layout) to the message loop, and reserves Escape for corrective passes only; keep the existing injected-start and Escape-first stop paths unchanged; unit tests cover all four entries, the Idle/Dictating transitions, and the exact layout-failure semantics (failed English confirmation leaves the core Idle, saves no session, injects no Win+H; the physical Win+H still proceeds natively because the hook never swallows).
- [ ] T3 Wire observed physical Win+H into `Toggle()`: while Idle use the race-start entry, while Dictating use the native-stop entry; the callback chains the physical event first and defers all stop work to the message loop so no Escape is injected before the chained event (a physical stop must never reopen the bar); add trace timestamps for press and layout-confirmed, and a best-effort bar-show timestamp (nullable: the TextInputHost popup show event is not observed for every launch); verify Ctrl+Alt+H still starts via injected `SendWinH` and never re-triggers the hook (injected guard).
- [ ] T4 Add tray checkbox "Intercept Win+H" (checked by default): live hook install/uninstall on toggle, menu status reflects state, hook absent while disabled, session-only state, disabled menu item inertness preserved for the status lines.
- [ ] T5 Update docs: revise the concept doc's physical Win+H interception exclusion into the opt-out feature (race variant, fail-open, tray toggle, Ctrl+Alt+H unchanged); update the repo AGENTS.md guardrail accordingly; create tracked `tmp/winh-interception-research.md` from the Research Evidence section in plan.md (self-contained; do not rely on any file outside the repository).
- [ ] T6 Run manual Windows verification matrix and record race evidence: Finnish and non-Finnish starting layouts (10 physical presses each; pass per press = Voice Typing observably using English via the bar's language indicator or a controlled English recognition check, at least 9/10 per layout; record only pass/fail metadata, never dictated content; English HKL confirmation, bar visibility via `IsVoiceUiVisible` (non-required due to false negatives), and timestamps are supporting evidence), plain Win and other Win combos unaffected, hold-and-release chord toggles exactly once, physical Win+H stop closes via the native handler with no injected Escape before the chained event and never reopens the bar, Ctrl+Alt+H stop keeps Escape-first behavior, failed English activation (native bar opens in current layout, core stays Idle), Ctrl+Alt+H with interception on and off, focus-loss self-heal, runtime toggle off/on, elevated-app limitation; record per-press outcomes and timestamps (press -> layout confirmed -> bar show, bar show nullable) in a committed file under `tmp/`.
