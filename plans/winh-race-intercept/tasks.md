# Tasks: Physical Win+H race interception

- [ ] T1 Add WH_KEYBOARD_LL hook scaffold: `SetWindowsHookExW`, `CallNextHookEx`, `UnhookWindowsHookEx`, `KBDLLHOOKSTRUCT`, and `GetKeyState` P/Invoke in Program.cs; a low-level callback that matches physical Win+H (vk `0x48`, Win modifier held), ignores `LLKHF_INJECTED` events, always returns 0, is exception-safe, and never inspects key content; trace events for hook install, uninstall, and each Win+H observation; uninstall in `CompleteShutdown`.
- [ ] T2 Add race-start entry to ToggleCore: performs the existing layout request and wait but skips `SendWinH`; keep the existing injected start unchanged; unit tests cover both start entries, the Idle/Dictating transitions, and fail-closed behavior when the English layout cannot be confirmed.
- [ ] T3 Wire observed physical Win+H into `Toggle()`: while Idle use the race-start entry, while Dictating use the existing stop path; add trace timestamps for press, layout-confirmed, and bar show; verify Ctrl+Alt+H still starts via injected `SendWinH` and never re-triggers the hook (injected guard).
- [ ] T4 Add tray checkbox "Intercept Win+H" (checked by default): live hook install/uninstall on toggle, menu status reflects state, hook absent while disabled, session-only state, disabled menu item inertness preserved for the status lines.
- [ ] T5 Update docs: revise the concept doc's physical Win+H interception exclusion into the opt-out feature (race variant, fail-open, tray toggle, Ctrl+Alt+H unchanged); update the repo AGENTS.md guardrail accordingly; copy `winh-interception-research.md` into `tmp/`.
- [ ] T6 Run manual Windows verification matrix and record race evidence: Finnish and non-Finnish starting layouts (10 physical presses each, English confirmed at least 9/10 per layout), plain Win and other Win combos unaffected, Win+H stop while dictating restores layout and focus, Ctrl+Alt+H with interception on and off, focus-loss self-heal, runtime toggle off/on, elevated-app limitation; record per-press outcomes and timestamps (press -> layout confirmed -> bar show) in a committed file under `tmp/`.
