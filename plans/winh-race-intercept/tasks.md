# Tasks: Physical Win+H race interception

- [ ] T1 Add WH_KEYBOARD_LL hook scaffold: `SetWindowsHookExW`, `CallNextHookEx`, `UnhookWindowsHookEx`, `KBDLLHOOKSTRUCT`, and `GetKeyState` P/Invoke in Program.cs; a low-level callback that matches physical Win+H (vk `0x48`, Win modifier held), ignores `LLKHF_INJECTED` events, chains through `CallNextHookEx` and returns its result on every pass-through path, handles `nCode < 0` by immediately chaining, never returns a nonzero value to swallow input, is exception-safe, and never inspects key content; exactly one observation per physical Win+H chord (dispatch on the first non-injected H keydown with Win held, suppress auto-repeat H keydowns, re-arm on H keyup or either Win keyup); trace events for hook install, uninstall, and each Win+H observation; uninstall in `CompleteShutdown`.
- [ ] T2 Add race-start entry to ToggleCore: performs the existing layout request and wait but skips `SendWinH`; keep the existing injected start unchanged; unit tests cover both start entries, the Idle/Dictating transitions, and the exact layout-failure semantics (failed English confirmation leaves the core Idle, saves no session, injects no Win+H; the physical Win+H still proceeds natively because the hook never swallows).
- [ ] T3 Wire observed physical Win+H into `Toggle()`: while Idle use the race-start entry, while Dictating use the existing stop path; add trace timestamps for press and layout-confirmed, and a best-effort bar-show timestamp (nullable: the TextInputHost popup show event is not observed for every launch); verify Ctrl+Alt+H still starts via injected `SendWinH` and never re-triggers the hook (injected guard).
- [ ] T4 Add tray checkbox "Intercept Win+H" (checked by default): live hook install/uninstall on toggle, menu status reflects state, hook absent while disabled, session-only state, disabled menu item inertness preserved for the status lines.
- [ ] T5 Update docs: revise the concept doc's physical Win+H interception exclusion into the opt-out feature (race variant, fail-open, tray toggle, Ctrl+Alt+H unchanged); update the repo AGENTS.md guardrail accordingly; create tracked `tmp/winh-interception-research.md` from the Research Evidence section in plan.md (self-contained; do not rely on any file outside the repository).
- [ ] T6 Run manual Windows verification matrix and record race evidence: Finnish and non-Finnish starting layouts (10 physical presses each; pass per press = English layout confirmed AND bar observed open via `IsVoiceUiVisible`, at least 9/10 per layout; bar-show timestamp best-effort, never required for pass), plain Win and other Win combos unaffected, hold-and-release chord toggles exactly once, Win+H stop while dictating restores layout and focus, failed English activation (native bar opens in current layout, core stays Idle), Ctrl+Alt+H with interception on and off, focus-loss self-heal, runtime toggle off/on, elevated-app limitation; record per-press outcomes and timestamps (press -> layout confirmed -> bar show, bar show nullable) in a committed file under `tmp/`.
