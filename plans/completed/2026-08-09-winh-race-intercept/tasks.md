# Tasks: Physical Win+H race interception

- [x] T1 Add WH_KEYBOARD_LL hook scaffold: `SetWindowsHookExW`, `CallNextHookEx`, `UnhookWindowsHookEx`, and `KBDLLHOOKSTRUCT` P/Invoke in Program.cs; a low-level callback that tracks `VK_LWIN`/`VK_RWIN` down/up state from the hook events themselves (never `GetKeyState`), matches physical Win+H (first non-injected H keydown while a tracked Win flag is set), ignores `LLKHF_INJECTED` events, suppresses auto-repeat H keydowns and re-arms on H keyup or either Win keyup (exactly one observation per chord), chains through `CallNextHookEx` and returns its result on every pass-through path, handles `nCode < 0` by immediately chaining, never returns a nonzero value to swallow input, is exception-safe, never inspects key content, and never injects input from the callback; the Idle race-start path performs a bounded synchronous layout request and confirmation (worst case ~200 ms, typical <5 ms) before chaining the H event, while the Dictating callback path never blocks (chains first, defers all stop work to the message loop); trace events for hook install, uninstall, and each Win+H observation; uninstall in `CompleteShutdown`.
- [x] T2 Add race-start and native-stop entries to ToggleCore: race-start performs the layout request and wait through a distinct bounded injected seam (100 ms `SendMessageTimeout` with `SMTO_ABORTIFHUNG`, then `WaitForLayout` ~100 ms cap, worst case ~200 ms) and skips `SendWinH`; the existing 1000 ms `RequestLayout` seam remains used only by the injected Ctrl+Alt+H start; native-stop arms the existing stop-confirmation watchdog, marks Idle, defers restoration (saved window and layout) to the message loop, and confirms the native close positively only: a bounded corrective Escape pass runs when the popup is observed visible or a SHOW event arrives while the stop confirmation is pending, absence is inconclusive and left to the existing watchdog expiry, and `OnVoiceUiShown` stays the reopen detector; keep the existing injected-start and Escape-first stop paths unchanged; unit tests cover all four entries, the distinct request seams, the Idle/Dictating transitions, the positive-only native-close confirmation, and the exact layout-failure semantics (failed English confirmation leaves the core Idle, saves no session, injects no Win+H; the physical Win+H still proceeds natively because the hook never swallows).
- [x] T3 Wire observed physical Win+H into `Toggle()`: while Idle use the race-start entry synchronously in the callback before chaining the H event (hook-safe bounded request path, worst case ~200 ms, typical <5 ms), while Dictating chain the physical event first and defer all stop work to the message loop so no Escape is injected before the chained event (a physical stop must never reopen the bar); add trace timestamps for press and layout-confirmed, and a best-effort bar-show timestamp (nullable: the TextInputHost popup show event is not observed for every launch); verify Ctrl+Alt+H still starts via injected `SendWinH` and never re-triggers the hook (injected guard).
- [x] T4 Add tray checkbox "Intercept Win+H" (checked by default): live hook install/uninstall on toggle, menu status reflects state, hook absent while disabled, session-only state, disabled menu item inertness preserved for the status lines.
- [x] T5 Update docs: revise the concept doc's physical Win+H interception exclusion into the opt-out feature (race variant, fail-open, tray toggle, Ctrl+Alt+H unchanged); update the repo AGENTS.md guardrail accordingly; create tracked `tmp/winh-interception-research.md` from the Research Evidence section in plan.md (self-contained; do not rely on any file outside the repository).
- [x] T6 Run manual Windows verification matrix and record race evidence: Finnish and non-Finnish starting layouts (10 physical presses each; pass per press = Voice Typing observably using English via the bar's language indicator or a controlled English recognition check, at least 9/10 per layout; record only pass/fail metadata, never dictated content; English HKL confirmation, bar visibility via `IsVoiceUiVisible` (non-required due to false negatives), and timestamps are supporting evidence), plain Win and other Win combos unaffected, hold-and-release chord toggles exactly once, physical Win+H stop closes via the native handler with no injected Escape before the chained event and never reopens the bar, native-close failure on positive evidence (popup visible or SHOW event after the stop: bounded corrective Escape pass closes the bar and restores layout and window within watchdog bounds; silently surviving bar with no observable popup signal documented as the accepted residual limitation), Ctrl+Alt+H stop keeps Escape-first behavior, failed English activation (native bar opens in current layout, core stays Idle), Ctrl+Alt+H with interception on and off, focus-loss self-heal, external close via physical Escape and Enter while dictating (layout and focus restored, core Idle, next start correct), runtime toggle off/on, elevated-app limitation; record per-press outcomes and timestamps (press -> layout confirmed -> bar show, bar show nullable) in a committed file under `tmp/`.
- [x] T7 Observe physical Escape, Enter, and Space while dictating: Escape runs the native-stop path (the user's key closes the bar; no injected keys, deferred restore, positive-only watchdog); Enter and Space are swallowed (the bar does not close on either natively, and the swallow prevents a stray newline or space in the target app) and defer the standard Escape-first stop to the message loop, closing the bar and restoring the saved state; Enter and Space close behavior is gated by tray checkboxes "Close dictation on Enter" and "Close dictation on Space" (both checked by default, session-only, live): while a toggle is off that key passes through untouched even while dictating; auto-repeat keydowns of either key are swallowed and their deferred stops no-op once the core is Idle; the injected guard keeps the app's own injected Escape from re-triggering; Enter and Space while Idle pass through untouched; unit coverage rides on the existing `StopDictationNative` and `StopDictation` tests; manual verification that Escape-close, Enter-close, and Space-close restore the saved layout and focus, leave the core Idle, insert no stray newline or space, leave Enter and Space untouched while Idle, and that unchecking/rechecking the Enter and Space toggles restores and removes the close behavior at runtime.

## Completion

- Completed at: 2026-08-09T14:46:03.572Z
- Mode: normal
