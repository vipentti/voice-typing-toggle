# Tasks: Eliminate the stop screen-flash

- [x] T1 Add temporary env-gated WinEvent instrumentation to Program.cs: `SetWinEventHook` (out-of-context) for foreground/show/hide/destroy events, window identity capture (HWND/PID/TID/class/title/visibility), and utility action markers (Escape sent, retry, RestoreFocus, layout confirmed); all output to the `VTT_EVENT_TRACE` path as CSV with high-resolution timestamps; no keystroke/dictated-text logging; zero behavior change when unset. Verify: `dotnet build` with 0 warnings, `dotnet publish` succeeds, smoke run writes a trace file, and a manual stop cycle appears in the trace.
- [x] T2 Collect user-driven measurement traces on the interactive desktop: VS Code with fi-FI and several windows stacked behind it (three short cycles plus one long cycle), Notepad, Windows Terminal, Firefox, Word; save traces under `.tmp/`. Verify: each case's trace shows the full stop sequence from Escape injection through restored layout, and the bar's window events are present.
- [x] T3 Analyze the traces and record findings in `.tmp/` notes: bar-window identity, event ordering and latencies (Escape → hide → destroy → foreground drop → MRU candidate → restore), boundary stability across apps, whether the shell re-wins after SetForegroundWindow; choose approach A (event-driven restore), B (focus-stability confirmation), or C (report-only) and justify with the data. Verify: analysis written, decision recorded with the supporting event timings, and no product code changed by this task.
- [x] T4 Implement approach D: make the Escape settle tunable via `VTT_ESCAPE_SETTLE_MS` (temporary, default 100 ms = known-good baseline) in ToggleCore/Program.cs, keeping the conditional second-Escape retry heuristic, focus-before-layout ordering, and the bounded fallback unchanged; run the tuning loop with the user (≥5 stop cycles per candidate settle 30/40/50/60 ms across VS Code with stacked windows, Notepad, Windows Terminal; watch for bar reopen, listening residue, focus/HKL correctness); hardcode the winning settle and remove the env var; remove the temporary `VTT_EVENT_TRACE` instrumentation from the final product. Add unit tests covering: settle value drives the sleep, conditional retry preserved, fallback unchanged. Verify: `dotnet build` 0 warnings, `dotnet test` green, `dotnet publish` succeeds, final source contains no `VTT_EVENT_TRACE` or `VTT_ESCAPE_SETTLE_MS` code, and the winning settle is recorded in `.tmp/` notes.
- [x] T6 Implement the stop watchdog (user-approved scope extension): `ToggleCore` gains `StopConfirmPending` (armed by `StopDictation`, cleared by the next `StartDictation`/heal, expiring after ~1.5 s via the 250 ms timer cadence), `OnVoiceUiShown` (bounded corrective stop sequence: Escape, settle, focus restore, layout restore; max 2 corrections), and a shared stop-sequence helper; `Program.cs` installs a permanent out-of-context `SetWinEventHook` (`EVENT_OBJECT_SHOW` only) matching the TextInputHost popup (class `Xaml_WindowedPopupClass`, title `PopupHost`, process `TextInputHost`) and forwards shows to the core; hook callback stays on the message-loop thread and statically rooted. Add unit tests: show while pending → corrective pass runs; show after a new start → ignored; expiry clears pending; correction bound respected. Verify: `dotnet build` 0 warnings, `dotnet test` green, `dotnet publish` succeeds.
- [x] T7 Fix the slow bar-launch race (user-approved; mechanism corrected after field failure of the blocking variant): `StartDictation` enters a `WaitingForBar` state confirmed by the 250 ms timer against an `EnumWindows` + `IsWindowVisible` poll (shared matcher; never blocks the message loop), a stop or focus change during launch runs the normal stop/heal path and arms the watchdog, and no-bar-in-time fails closed (restore layout, Idle, watchdog armed); watchdog pending window ~2.5 s. Unit tests: launch confirms when the bar appears, fail-closed path restores layout and arms the watchdog, stop-during-launch runs the stop path and arms the watchdog, normal start unaffected. Verify: `dotnet build` 0 warnings, `dotnet test` green, `dotnet publish` succeeds.
- [x] T5 Run manual acceptance on the interactive desktop at the winning settle value: no visible background-window flash on stop for the stacked-VS Code case and the other required apps; bar closed and stays closed with no delayed reopen; focus returns to the exact saved window and stays (≥2 s); exact saved HKL restored and stable (≥2 s); Notepad/Windows Terminal first-Escape consumption still handled; focus-loss heal and shutdown restoration work; repeated toggles and a long cycle behave. Verify: all acceptance criteria pass, results recorded in `.tmp/` notes, and `docs/voice-typing-toggle-concept.md` updated only if stop semantics changed.

## Completion

- Completed at: 2026-08-08T12:55:12.596Z
- Mode: normal
