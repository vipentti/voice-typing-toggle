# Tasks: Eliminate the stop screen-flash

- [ ] T1 Add temporary env-gated WinEvent instrumentation to Program.cs: `SetWinEventHook` (out-of-context) for foreground/show/hide/destroy events, window identity capture (HWND/PID/TID/class/title/visibility), and utility action markers (Escape sent, retry, RestoreFocus, layout confirmed); all output to the `VTT_EVENT_TRACE` path as CSV with high-resolution timestamps; no keystroke/dictated-text logging; zero behavior change when unset. Verify: `dotnet build` with 0 warnings, `dotnet publish` succeeds, smoke run writes a trace file, and a manual stop cycle appears in the trace.
- [ ] T2 Collect user-driven measurement traces on the interactive desktop: VS Code with fi-FI and several windows stacked behind it (three short cycles plus one long cycle), Notepad, Windows Terminal, Firefox, Word; save traces under `.tmp/`. Verify: each case's trace shows the full stop sequence from Escape injection through restored layout, and the bar's window events are present.
- [ ] T3 Analyze the traces and record findings in `.tmp/` notes: bar-window identity, event ordering and latencies (Escape → hide → destroy → foreground drop → MRU candidate → restore), boundary stability across apps, whether the shell re-wins after SetForegroundWindow; choose approach A (event-driven restore), B (focus-stability confirmation), or C (report-only) and justify with the data. Verify: analysis written, decision recorded with the supporting event timings, and no product code changed by this task.
- [ ] T4 Implement the chosen approach: if A, add a `WaitForVoiceUiClosed`-style seam to ToggleCore replacing the blind `EscapeRetryMs` sleep and foreground-based retry heuristic, wire the WinEvent hook in Program.cs, keep focus-before-layout ordering and the bounded fallback; if B, add focus-stability confirmation and one reattach/retry; remove the temporary trace instrumentation from the final product. Add unit tests covering: event fires → immediate restore, no event → bounded fallback, no event → second-Escape retry, focus-stability retry (B). Verify: `dotnet build` 0 warnings, `dotnet test` green, `dotnet publish` succeeds, final source contains no `VTT_EVENT_TRACE` code.
- [ ] T5 Run manual acceptance on the interactive desktop: no visible background-window flash on stop for the stacked-VS Code case and the other required apps; bar closed and stays closed with no delayed reopen; focus returns to the exact saved window and stays (≥2 s); exact saved HKL restored and stable (≥2 s); Notepad/Windows Terminal first-Escape consumption still handled; focus-loss heal and shutdown restoration work; repeated toggles and a long cycle behave. Verify: all acceptance criteria pass, results recorded in `.tmp/` notes, and `docs/voice-typing-toggle-concept.md` updated only if stop semantics changed.
